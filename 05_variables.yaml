- name: variables
  hosts: localhost
  connection: local
  tasks:
  - name: print the variable
    ansible.builtin.debug:
      msg: "course is DevOps with AWS, Trainer is Siva, Duration is 120hrs"
      